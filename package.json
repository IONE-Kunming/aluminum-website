{
  "name": "aluminum-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "IONE AlumaTech - Vanilla JavaScript aluminum trading platform",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "create-admin": "node create-admin.js",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:all": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.7.1",
    "html2pdf.js": "^0.14.0",
    "lucide": "^0.575.0"
  },
  "devDependencies": {
    "firebase-admin": "^13.6.1",
    "terser": "^5.46.0",
    "vite": "^7.2.4"
  }
}
