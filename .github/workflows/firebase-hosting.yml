name: Deploy to Firebase

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build
      run: npm run build
    
    - name: Setup Firebase
      run: |
        npm install -g firebase-tools
        echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > service-account.json
        
    - name: Deploy
      env:
        GOOGLE_APPLICATION_CREDENTIALS: service-account.json
      run: |
        # Deploy only firestore rules, indexes, and hosting
        # Skip storage deployment to avoid bucket permission errors
        # Storage rules are kept in firebase.json for future use when bucket is properly configured
        # Use --force flag to skip rule validation which requires firebaserules.googleapis.com API permissions
        firebase deploy --only firestore:rules,firestore:indexes,hosting --project gen-lang-client-0988357303 --non-interactive --force
